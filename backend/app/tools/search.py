"""Web search tool for looking up current pricing, technologies, and best practices."""


def web_search(query: str) -> str:
    """Search the web for current information.

    Placeholder — replace with actual search API (Tavily, SerpAPI, etc.).
    """
    return f"[Search results for: {query}]"
